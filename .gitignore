.venv
.venv.nosync/
__pycache__/
*.pyc
.tile_cache/

# Raw radio captures — debugging output, one per connection.
# Flight_*.csv is deliberately NOT ignored: it is the graded deliverable and
# should stay visible in `git status` so a real flight never goes uncommitted.
raw_*.log

# Rehearsal recordings from ground_station_sim.py — never a deliverable.
SIM_Flight_*.csv
